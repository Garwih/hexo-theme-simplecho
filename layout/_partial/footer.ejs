  <footer id="footer">
    <div class="foot-warp">
      <p>© 2014-2016 <%= config.title %></p>
    </div>
  </footer>
  <% if (!is_archive()){ %>
  <% if (config.disqus_shortname){ %>
  <script>
    <% if(is_post()) { %>
      var disqus_shortname = '<%= config.disqus_shortname %>';
      <% if (page.permalink){ %>
      var disqus_url = '<%= page.permalink %>';
      <% } %>
    <% } else if(theme.disqus_home) { %>
      var disqus_home = true;
      var disqus_shortname = '<%= config.disqus_shortname %>';
    <% } %>
  </script>
  <% } else if ((theme.duoshuo_shortname && theme.duoshuo_home) || (theme.duoshuo_shortname && is_post())) { %>
  <script type="text/javascript">
    var duoshuoQuery = {short_name:'<%= theme.duoshuo_shortname %>'};
  </script>
  <% } %>
  <% } %>
  <script src="<%- url_for() %>js/tools.js"></script>  